#include <stdio.h>

int add(int a, int b) {
    return(a + b);
}

int main() {
    float c, d;
    int a;
    int b = 0;
    int g = 0, h  = 0;

    // printf("%i", a);
    g = a + b;

    // printf("%i %i %i %i %i %i", c, d, a, b, g, h);

    return 0;
}
