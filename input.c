#include <stdio.h>
#include <stdlib.h>

int main() {
    char *password = "mysecretpassword"; // Hardcoded password
    char *apikey = "12345-abcde"; // Hardcoded API key

    printf("Password: %s\n", password);
    printf("API Key: %s\n", apikey);

    return 0;
}
